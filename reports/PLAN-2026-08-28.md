# Plan — Thursday, August 28, 2026

The evening report (`reports/2026-08-28.md`) will grade the day against this list.

1. **Land W84** — a Controller can hold a deliverable with a typed reason; Controller or Approver
   releases; both screens show it. (Ruled yesterday.)
2. **Land W85** — the "monitor" kind enters the data model and The Watch reads real monitors: the
   Today screens reach six of six, and the demo record shows it.
3. **Land W83** — submit-to-review, already built and locally green; needs main merged in and one
   clean CI run (its reds were runner flakes).
4. **Adjudicate the W85 duplicate** (PR 116 vs 117) and keep the queue two windows deep at every
   pass, with W86+ aimed at whichever of these is not yet closed.
5. **Runner health**: if the interrupted-test flake recurs on any PR, diagnose on the Mac (memory
   pressure inside the Colima virtual machine is the suspect) rather than rerunning a third time.
6. **Decisions to place before Zusha** (carry until answered): catalog-import restart date;
   which ledger system and entity hold the first real books.

Stretch, only if all of the above closes: author the browser-served proof gate window (a check
that starts the app and makes a real HTTP request, so green means browser-real, not just
server-rendered).
